import 'package:flutter/material.dart';

class LoginAdmPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("adm login page")),
    );
  }
}
